import { Routes } from '@angular/router';
import { ShowSlideshowComponent } from './components/show-slideshow/show-slideshow.component';

export const routes: Routes = [
    { path: '', component: ShowSlideshowComponent }
];
